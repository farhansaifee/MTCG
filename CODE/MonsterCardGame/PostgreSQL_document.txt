create table if not exists users
(
	id integer,
	username varchar(50) unique,
	password varchar(30),
	coins integer
);

/* Falls owner nicht postgres ist: */
alter table users owner to postgres;

create table if not exists card
(
	id integer,
	name varchar(256),
	damage integer,
	element varchar(256),
	type varchar(256)
);